{
  "name": "finance-control",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "src/web",
    "src/core"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w src/web\" \"npm run dev -w src/core\"",
    "dev:web": "npm run dev -w src/web",
    "dev:core": "npm run dev -w src/core"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
